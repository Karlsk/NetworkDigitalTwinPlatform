# 服务全局配置
neo4j:
  uri: "bolt://localhost:7687"
  user: "neo4j"
  password: "password123"
  default_db: "neo4j"

server:
  port: 8080

snapshot:
  dir: "snapshots"
  max_active: 5

schema:
  ontology_dir: "ontology"

channel:
  buffer_size: 100
