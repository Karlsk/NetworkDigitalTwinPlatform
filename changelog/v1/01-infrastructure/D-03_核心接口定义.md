# D-03: 核心接口定义 (SchemaRegistry / Connector / GraphDB)

## 1. 任务概述

定义系统三大核心接口契约：SchemaRegistry（本体注册表）、Connector（数据源适配器）、GraphDB（图数据库驱动）。这些接口是后续所有实现任务的契约，确保模块间解耦。

| 属性 | 值 |
|------|-----|
| 所属阶段 | Phase 1: 设计阶段 |
| 预估工时 | 1.5 天 |
| 前置任务 | D-01 |
| 交付物 | `internal/schema/registry.go`、`internal/connector/interface.go`、`internal/graph/interface.go` |

## 2. 详细实现步骤

### Day 1: SchemaRegistry + Connector 接口

#### SchemaRegistry 接口

**文件**: `internal/schema/registry.go`

```go
package schema

// SchemaRegistry 本体注册表接口
// 系统启动时加载 ontology/ 目录下所有 YAML，提供 EntityType 和 RelationType 的查询能力
type SchemaRegistry interface {
    // Load 加载目录下所有 YAML 文件（支持多文档 YAML）
    Load(dir string) error

    // GetEntityType 按名称获取 EntityType
    GetEntityType(name string) (*EntityType, error)

    // GetRelationType 按名称获取 RelationType
    GetRelationType(name string) (*RelationType, error)

    // ListEntityTypes 列出所有 EntityType
    ListEntityTypes() []*EntityType

    // ListRelationTypes 列出所有 RelationType
    ListRelationTypes() []*RelationType

    // Validate 校验数据合法性（属性类型/必填/枚举/stableKeys 非空/默认值填充）
    Validate(entityKind string, props map[string]any) error
}
```

#### Connector 接口

**文件**: `internal/connector/interface.go`

```go
package connector

import "context"

// Connector 数据源适配器接口
// 每个数据源（Netbox/Controller/CMDB）实现一个 Connector
type Connector interface {
    // Metadata 返回连接器元信息
    Metadata() ConnectorMetadata

    // Collect 全量拉取指定实体类型的数据
    Collect(ctx context.Context, entityType string) ([]Resource, error)

    // Stream 流式推送增量变更（MVP 返回 ErrNotImplemented，V1 接入 Kafka）
    Stream(ctx context.Context, entityType string) (<-chan Resource, error)
}

// ConnectorRegistry 连接器注册中心
type ConnectorRegistry struct {
    connectors map[string]Connector
}

func (r *ConnectorRegistry) Register(c Connector)
func (r *ConnectorRegistry) Get(name string) (Connector, error)
func (r *ConnectorRegistry) List() []ConnectorMetadata
```
[T-02_to_T-05_单元测试与集成测试.md](T-02_to_T-05_%E5%8D%95%E5%85%83%E6%B5%8B%E8%AF%95%E4%B8%8E%E9%9B%86%E6%88%90%E6%B5%8B%E8%AF%95.md)
### Day 2 (半天): GraphDB 接口

**文件**: `internal/graph/interface.go`

```go
package graph

import (
    "context"

    "gitlab.com/pml/network-digital-twin/internal/assembler"
)

// GraphDB 图数据库驱动接口
// 所有操作带 db 参数（逻辑多 DB），Cypher 生成内聚在实现中
type GraphDB interface {
    // Ping 检查连接
    Ping(ctx context.Context) error

    // Close 关闭连接
    Close() error

    // === 全量同步 ===

    // BulkCreate 批量 CREATE（先 ClearDB 再 BulkCreate）
    BulkCreate(ctx context.Context, db string, nodes []assembler.Node, rels []assembler.Relation) error

    // === 增量同步 ===

    // Upsert MERGE 节点 + SET += 属性增量合并 + MERGE 关系
    Upsert(ctx context.Context, db string, nodes []assembler.Node, rels []assembler.Relation) error

    // DeleteRelations 仅删除指定关系
    DeleteRelations(ctx context.Context, db string, rels []assembler.Relation) error

    // DeleteByURIs 按 URI 删除节点 + DETACH DELETE 关联关系
    DeleteByURIs(ctx context.Context, db string, uris []string) error

    // === 查询 ===

    // Query 执行 Cypher 查询（驱动层自动注入 $_db）
    Query(ctx context.Context, db string, cypher string, params map[string]any) ([]map[string]any, error)

    // BuildCypher 预览生成的 Cypher 语句（不执行），用于测试/audit/调试
    // action: "create", "upsert", "delete", "delete_relations"
    BuildCypher(action string, db string, nodes []assembler.Node, rels []assembler.Relation, uris []string) (string, map[string]any)

    // === 逻辑 DB 管理 ===

    // ClearDB 清空指定逻辑 DB 的所有数据
    ClearDB(ctx context.Context, db string) error

    // CloneDB 将一个逻辑 DB 完整复制到另一个
    CloneDB(ctx context.Context, from, to string) error

    // ListDBs 列出所有逻辑 DB
    ListDBs(ctx context.Context) ([]string, error)

    // HasDB 判断逻辑 DB 是否存在数据
    HasDB(ctx context.Context, db string) (bool, error)
}
```

## 3. 设计原理

### 为什么 SchemaRegistry 是接口而非 struct？

- 后续可能需要不同的实现（如远程 Schema 服务、数据库存储）
- 测试时可以 Mock 实现，不依赖真实 YAML 文件
- Go 中 struct → interface 的演进时机：当有第二个消费者或需要 Mock 时就提取接口

### 为什么 GraphDB 没有独立的 Builder？

- Builder 职责（Cypher 生成）已合并到 GraphDB 实现中，避免两层拆分导致的职责重叠
- `BuildCypher` 方法暴露 Cypher 预览能力，用于测试和 audit
- 换图数据库 → 新建 GraphDB 实现，内部生成对应查询语言（如 ArangoDB 的 AQL）

### Connector 的 Collect 与 Stream 分离

- `Collect`：全量拉取，用于 FullSync
- `Stream`：流式推送，用于增量同步。MVP 返回 `ErrNotImplemented`，V1 接入 Kafka
- 两个方法分离让 Connector 可以只实现 Collect（MVP），不影响 Stream 的扩展性

### 与其他模块的交互

```
SchemaRegistry ← Normalizer (读 EntityType)
SchemaRegistry ← GraphAssembler (读 relationFields + RelationType)
SchemaRegistry ← Validator (读 PropertySpec)
Connector      → SyncService (提供 Resource)
GraphDB        ← SyncService (写入图数据)
GraphDB        ← SnapshotManager (快照导出/导入)
GraphDB        ← MCP Server (查询拓扑)
```

## 4. 验收标准

- [ ] 编译期类型检查通过，`go build ./...` 无错误
- [ ] SchemaRegistry 接口包含 Load、Get、List、Validate 共 6 个方法
- [ ] Connector 接口包含 Metadata、Collect、Stream 共 3 个方法
- [ ] ConnectorRegistry 包含 Register、Get、List 共 3 个方法
- [ ] GraphDB 接口包含 12 个方法（Ping/Close/BulkCreate/Upsert/DeleteRelations/DeleteByURIs/Query/BuildCypher/ClearDB/CloneDB/ListDBs/HasDB）
- [ ] 所有接口方法签名与架构设计.md 一致
- [ ] GraphDB 的所有写操作方法均带 `db string` 参数

## 5. 注意事项

- GraphDB 接口中 Node 和 Relation 类型引用 `internal/assembler` 包，形成 GraphModel IR 的统一定义
- `BuildCypher` 的 action 参数使用字符串（"create"/"upsert"/"delete"/"delete_relations"），不用枚举，保持简单
- Connector.Stream() MVP 阶段返回 `ErrNotImplemented`，不需要 panic
- `SchemaRegistry.Validate` 方法的返回值应包含具体的校验失败信息（字段名、期望值、实际值）
- ConnectorRegistry 的 `connectors` map 以 Connector 的 `Metadata().Name` 为 key
- 接口文件只定义签名，不包含实现逻辑
