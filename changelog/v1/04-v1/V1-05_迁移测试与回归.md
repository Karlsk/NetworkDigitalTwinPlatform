# V1-05: 迁移测试 + 回归验证

**工时**: 1 天
**前置**: V1-01, V1-03, V1-04
**风险等级**: 中
**Phase**: Phase 1 — 基础迁移

---

## 背景

Phase 1 包含一个 BREAKING CHANGE（V1-01 Node.Labels）和两个新模块（V1-03 Factory、V1-04 HTTPClient），需要全量回归验证确保不破坏现有功能。

---

## 实现内容

### 1. 全量回归测试

```bash
go build ./...
go test -race -cover ./...
```

修复因 `Node.Labels` 改造导致的所有测试失败，重点关注：
- `internal/assembler/assembler_test.go` — 节点结构断言
- `internal/graph/neo4j_test.go` — Cypher 断言
- `internal/snapshot/manager_test.go` — 快照导入/导出断言
- `internal/mcp/mcp_test.go` — MCP 工具测试
- `test/e2e/e2e_test.go` — 端到端测试

### 2. 新增测试

#### ConnectorFactory 集成测试 (connector/factory_test.go)

- TC-F01: RegisterBuilder + Create 正常路径
- TC-F02: 未注册 type 返回清晰错误
- TC-F03: CreateFromConfig 从 YAML 创建 Mock Connector
- TC-F04: CreateFromConfig 文件不存在返回错误

#### Connector 配置测试 (connector/config_test.go)

- TC-CF01: LoadConnectorConfig 正确解析 YAML
- TC-CF02: AuthConfig 各字段正确解析
- TC-CF03: 缺失必要字段（name/type）返回错误

#### HTTPClient 测试 (connector/httpclient_test.go)

已在 V1-04 中定义，本任务确保全部通过。

### 3. 覆盖率检查

```bash
go test -coverprofile=coverage.out ./...
go tool cover -func=coverage.out | tail -1
# 目标: >= 70%
```

### 4. Lint 检查 (可选，依赖 golangci-lint 安装)

```bash
golangci-lint run
# 目标: 无 Error（Warning 可接受）
```

---

## 涉及文件清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `internal/connector/factory_test.go` | 新建 | Factory 测试 |
| `internal/connector/config_test.go` | 新建 | Config 加载测试 |
| `internal/assembler/assembler_test.go` | 修改 | 适配 Node.Labels |
| `internal/assembler/types_test.go` | 修改 | 适配 Node.Labels |
| `internal/graph/neo4j_test.go` | 修改 | Cypher 断言更新 |
| `internal/snapshot/manager_test.go` | 修改 | 快照测试适配 |
| `internal/mcp/mcp_test.go` | 修改 | MCP 测试适配 |
| `test/e2e/e2e_test.go` | 修改 | E2E 测试适配 |
| `coverage.out` | 修改 | 覆盖率报告 |

---

## 验收标准

- [x] `go build ./...` 编译通过
- [x] `go test ./...` 全部通过，0 failures
- [x] `go test -race ./...` 无 data race
- [x] 覆盖率 >= 70%
- [x] `ConnectorFactory` 集成测试全部通过
- [x] `ConnectorConfig` 加载测试全部通过
- [x] `HTTPClient` 测试全部通过
- [x] `golangci-lint run` 无 Error（如已安装）
