# V1-10: 全链路验证 (真实 Connector + FullSync)

**工时**: 1 天
**前置**: V1-09
**风险等级**: 中
**Phase**: Phase 2a — 真实数据源 Connector

---

## 背景

三个真实 Connector 均已实现并通过单元测试。本任务验证 Connector → Normalizer → Assembler → GraphDB → Neo4j 完整数据流管线，确保真实数据源与现有同步流水线完全兼容。

---

## 实现内容

### 1. E2E 测试更新 (test/e2e/e2e_test.go)

新增使用 httptest mock server 的 E2E 测试用例：

```go
func TestE2E_FullSyncWithRealConnectors(t *testing.T) {
    // 1. 启动 httptest mock server，模拟 Netbox + CMDB API
    netboxServer := setupMockNetboxServer()
    cmdbServer := setupMockCMDBServer()
    defer netboxServer.Close()
    defer cmdbServer.Close()

    // 2. 通过 ConnectorFactory 创建真实 Connector
    factory := connector.NewConnectorFactory()
    // 注册 netbox/cmdb builder（使用 mock server URL）

    // 3. 执行全量同步 FullSync
    // 4. 验证 Neo4j 中节点/关系数正确
    // 5. 验证节点属性正确（来自真实 API 的展平字段）
}
```

### 2. pipeline-demo 适配 (cmd/pipeline-demo/main.go)

更新 pipeline-demo 演示使用 ConnectorFactory 创建 Connector 的流程：

```go
// 通过配置文件创建 Connector（无需硬编码）
factory := connector.NewConnectorFactory()
if err := factory.CreateFromConfig("configs/connectors.yaml", connRegistry); err != nil {
    log.Fatal(err)
}
```

### 3. Golden File 验证

- 验证全量同步后的 Neo4j 数据与 `testdata/golden/expected_topology.yaml` 一致
- 如真实 Connector 输出格式有差异，需更新 Golden File

---

## 涉及文件清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `test/e2e/e2e_test.go` | 修改 | 新增真实 Connector E2E 测试 |
| `cmd/pipeline-demo/main.go` | 修改 | 适配 ConnectorFactory |
| `testdata/golden/expected_topology.yaml` | 可能修改 | 如格式有差异需更新 |

---

## 验收标准

- [x] E2E 测试使用 httptest mock server 通过完整同步流水线
- [x] Neo4j 中节点数/关系数与预期一致
- [x] 节点属性来自真实 API 的展平字段（非 Mock 数据）
- [x] pipeline-demo 可通过配置切换数据源
- [x] `go test -race ./test/e2e/...` 全部通过
